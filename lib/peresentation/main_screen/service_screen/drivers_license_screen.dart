import 'package:flutter/material.dart';

class DriversLicenseScreen extends StatelessWidget {
  const DriversLicenseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Haydovchilik guvohnomasi"),
        ),
      ),
    );
  }
}
