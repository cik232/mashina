import 'package:flutter/material.dart';

class RetrofittingScreen extends StatelessWidget {
  const RetrofittingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Qayta jihozlash"),
        ),
      ),
    );
  }
}
