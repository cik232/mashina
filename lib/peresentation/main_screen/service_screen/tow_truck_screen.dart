import 'package:flutter/material.dart';

class TowTruckScreen extends StatelessWidget {
  const TowTruckScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Evakuvator chaqirish"),
        ),
      ),
    );
  }
}
