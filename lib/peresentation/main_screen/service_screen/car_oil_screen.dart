import 'package:flutter/material.dart';

class CarOilScreen extends StatelessWidget {
  const CarOilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Automobil moyi"),
        ),
      ),
    );
  }
}
