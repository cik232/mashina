import 'package:flutter/material.dart';

class GazScreen extends StatelessWidget {
  const GazScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Gaz"),
        ),
      ),
    );
  }
}
