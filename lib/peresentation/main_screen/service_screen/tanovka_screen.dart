import 'package:flutter/material.dart';

class TanovkaScreen extends StatelessWidget {
  const TanovkaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Tanirofka"),
        ),
      ),
    );
  }
}
