import 'package:flutter/material.dart';

class FineScreen extends StatelessWidget {
  const FineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Jarimalar"),
        ),
      ),
    );
  }
}
