import 'package:flutter/material.dart';

class BtnbarSettings extends StatelessWidget {
  const BtnbarSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
