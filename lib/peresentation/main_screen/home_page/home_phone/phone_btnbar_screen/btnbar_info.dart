import 'package:flutter/material.dart';

class BtnbarInfo extends StatelessWidget {
  const BtnbarInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Container(),
    );
  }
}
